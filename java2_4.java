package java2_4;
// 새로 정의할 클래스 이름 기술
public class java2_4 {
	// 메인 메서드 기술
	public static void main(String[] srgs) {
		// 번수 선언하여 값 할당
		int a = 8;
		int b = 5;
		
		System.out.println("a + b = " + (a + b)); // 결과를 모니터에 출력 (a+b)
		System.out.println("a - b = " + (a - b)); // 결과를 모니터에 출력 (a+b)
		System.out.println("a * b = " + (a * b)); // 결과를 모니터에 출력 (a+b)
		System.out.println("a / b = " + (a / b)); // 결과를 모니터에 출력 (a+b)
		System.out.println("a % b = " + (a % b)); // 결과를 모니터에 출력 (a+b)
	}

}
